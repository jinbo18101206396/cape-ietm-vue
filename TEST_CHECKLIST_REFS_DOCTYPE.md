# §16.4 重建 refs 与 DOCTYPE 测试检查清单

## 测试环境
- 前端：http://localhost:3000
- 后端：http://localhost:9999（或实际端口）
- 测试日期：2026-08-10

---

## P0 核心流程测试（必测）

### 测试用例 1：正常场景 - 有 dmRef 有 graphic
**前置条件**：
- 打开一个包含 dmRef 和 graphic 的 DM
- DM 内容示例：
  ```xml
  <dmodule>
    <content>
      <description>
        <para>参考 <dmRef>...</dmRef></para>
        <figure>
          <graphic infoEntityIdent="ICN-001"/>
        </figure>
      </description>
    </content>
  </dmodule>
  ```

**操作步骤**：
1. 点击工具栏"重建Refs"按钮（图标 sync）
2. 观察是否弹出确认框，标题"重建 refs 与 DOCTYPE"
3. 点击"确定"
4. 观察是否有后续弹框（补 ICN 后缀）
5. 如果有补后缀框，选择后缀（如 .cgm）并确定
6. 观察提示消息

**预期结果**：
- [x] 弹出确认框，内容包含"将删除原 content/refs 元素..."
- [x] 确认后开始处理
- [x] 如果 ICN 无后缀，弹出补后缀框，列出 ICN 清单
- [x] 补后缀后提示"生成成功。"
- [x] XML 中出现 `<content><refs>` 块，包含 dmRef
- [x] XML 顶部 DOCTYPE 包含 `<!NOTATION cgm ...>` 和 `<!ENTITY ICN-001 ...>`
- [x] 编辑器标记为"未保存"（dirty）

**检查点**：
```javascript
// 在浏览器控制台检查 icnlist
console.log('icnlist:', window.vue.$children[0].icnlist)

// 检查 nodeList 是否刷新
console.log('nodeList 长度:', window.vue.$children[0].nodeList.length)
```

---

### 测试用例 2：边界场景 - 无 dmRef
**前置条件**：打开一个只有 graphic 无 dmRef 的 DM

**操作步骤**：
1. 点击"重建Refs"
2. 确认

**预期结果**：
- [x] 不生成 refs 块（或生成空 `<refs/>`）
- [x] DOCTYPE 仍然生成（包含 ICN 的 ENTITY）
- [x] 提示"生成成功。"

---

### 测试用例 3：边界场景 - 无 graphic
**前置条件**：打开一个只有 dmRef 无 graphic 的 DM

**操作步骤**：
1. 点击"重建Refs"
2. 确认

**预期结果**：
- [x] 生成 refs 块
- [x] DOCTYPE 为 `<!DOCTYPE dmodule[]>`（空实体）
- [x] 提示"生成成功。"

---

### 测试用例 4：中英文视图切换
**前置条件**：打开一个有 dmRef 的 DM，切换到中文视图

**操作步骤**：
1. 切换到中文视图（工具栏"中文"按钮）
2. 点击"重建Refs"
3. 确认
4. 观察 refs 块的元素名

**预期结果**：
- [x] refs 块用中文元素名（如 `<引用>` `<数据模块引用>`）
- [x] 切换回英文视图 → refs 块恢复英文元素名
- [x] 保存后查看数据库 → XML 恒为英文

---

### 测试用例 5：用户取消补后缀
**前置条件**：打开一个有无后缀 ICN 的 DM

**操作步骤**：
1. 点击"重建Refs"
2. 确认主弹框
3. 补后缀弹框出现后点击"取消"

**预期结果**：
- [x] 提示"取消生成。"
- [x] XML 未改变
- [x] 编辑器保持原状态

---

## P1 性能测试（推荐）

### 测试用例 6：大 XML 文件性能
**前置条件**：打开一个 1000+ 行、100+ 个 dmRef 的大 DM

**操作步骤**：
1. 打开浏览器 Performance 面板
2. 开始录制
3. 点击"重建Refs"并确认
4. 停止录制

**预期结果**：
- [x] 总耗时 < 2 秒
- [x] 无明显卡顿
- [x] 内存占用无异常增长

**检查方式**：
```javascript
// 在控制台手动计时
console.time('重建Refs')
// 点击按钮...
console.timeEnd('重建Refs')  // 应 < 2000ms
```

---

### 测试用例 7：loadDm 性能影响
**前置条件**：打开一个包含 100 个 ENTITY 的 DM

**操作步骤**：
1. 在控制台监控加载时间
2. 刷新页面重新加载 DM

**预期结果**：
- [x] loadDm 耗时增加 < 10ms
- [x] icnlist 正确解析（100 个元素）

**检查方式**：
```javascript
// 在 DmContentEditor.vue loadDm 中临时添加：
console.time('parseIcnlist')
this._parseIcnlistFromXml()
console.timeEnd('parseIcnlist')  // 应 < 10ms
```

---

## P2 集成测试（可选）

### 测试用例 8：重建后保存
**操作步骤**：
1. 重建 refs 与 DOCTYPE
2. 点击"保存"
3. 刷新页面重新加载

**预期结果**：
- [x] 保存成功（无乐观锁冲突）
- [x] 重新加载后 refs 块和 DOCTYPE 保持
- [x] icnlist 从 DOCTYPE 正确解析

---

### 测试用例 9：重建后校验
**操作步骤**：
1. 重建 refs 与 DOCTYPE
2. 点击"校验"

**预期结果**：
- [x] 校验通过（或至少不增加新错误）
- [x] dmRef 相关错误减少
- [x] ENTITY 引用错误消失

---

### 测试用例 10：重建后预览
**操作步骤**：
1. 重建 refs 与 DOCTYPE
2. 点击"预览"

**预期结果**：
- [x] 预览正常显示
- [x] 图形正确渲染（ICN 找到文件）
- [x] dmRef 超链接可点击

---

## 异常场景测试

### 测试用例 11：空 XML
**操作步骤**：
1. 清空编辑器内容
2. 点击"重建Refs"

**预期结果**：
- [x] 提示"内容为空"或静默忽略（不报错）

---

### 测试用例 12：格式错乱的 XML
**操作步骤**：
1. 手动破坏 XML 格式（如删除闭合标签）
2. 点击"重建Refs"

**预期结果**：
- [x] 捕获异常，提示友好错误消息
- [x] 不崩溃

---

### 测试用例 13：网络断开
**操作步骤**：
1. 断开网络
2. 点击"重建Refs"

**预期结果**：
- [x] 功能正常（纯前端逻辑，不依赖后端）
- [x] 保存时才提示网络错误

---

## 控制台检查

### 无错误日志
打开浏览器控制台（F12），执行操作过程中：
- [ ] 无红色 Error
- [ ] 无黄色 Warning（或仅有无关警告）
- [ ] console.log 输出符合预期

### Vue Devtools 检查
安装 Vue Devtools 扩展：
- [ ] icnlist 数组正确填充
- [ ] nodeList 长度正确
- [ ] dirty 状态正确切换

---

## 回归测试（确保不影响现有功能）

### 其他按钮仍正常
- [ ] 保存按钮
- [ ] 校验按钮
- [ ] 预览按钮
- [ ] 格式化按钮
- [ ] 插入图符按钮
- [ ] 插入 dmRef 按钮

### 三区联动正常
- [ ] 点击树节点 → 编辑器光标跳转
- [ ] 编辑器移动光标 → 树节点高亮
- [ ] 属性面板正确显示

---

## 测试结果记录

| 用例 | 状态 | 备注 |
|------|------|------|
| TC-01 正常场景 | ⬜ 待测 | |
| TC-02 无 dmRef | ⬜ 待测 | |
| TC-03 无 graphic | ⬜ 待测 | |
| TC-04 中英文视图 | ⬜ 待测 | |
| TC-05 取消补后缀 | ⬜ 待测 | |
| TC-06 大文件性能 | ⬜ 待测 | |
| TC-07 loadDm 性能 | ⬜ 待测 | |
| TC-08 保存集成 | ⬜ 待测 | |
| TC-09 校验集成 | ⬜ 待测 | |
| TC-10 预览集成 | ⬜ 待测 | |
| TC-11 空 XML | ⬜ 待测 | |
| TC-12 格式错乱 | ⬜ 待测 | |
| TC-13 网络断开 | ⬜ 待测 | |

**测试通过标准**：P0 核心流程 5/5 通过 + P1 性能 2/2 通过 + 无阻塞性 bug

---

## 发现问题时的信息收集

如果测试失败，请提供：
1. **控制台错误信息**（红色 Error 完整文本）
2. **操作步骤**（从哪个页面、点了什么按钮）
3. **预期 vs 实际**（应该怎样 vs 实际怎样）
4. **截图**（弹框、错误提示）
5. **XML 片段**（出问题的 DM 的 dmRef/graphic 部分）

我会立即分析并修复。
