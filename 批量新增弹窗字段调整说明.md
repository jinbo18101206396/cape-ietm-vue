# 批量新增弹窗字段调整说明

## 📝 修改内容

根据用户要求，从批量新增弹窗中移除了以下4个字段：

### 移除的字段

1. ❌ **构型节点** - 已移除（用户已在树上选择，无需重复显示）
2. ❌ **创作单位名称** - 已移除（保留编码即可）
3. ❌ **责任单位名称** - 已移除（保留编码即可）
4. ❌ **安全等级** - 已移除（可选字段，简化表单）

### 保留的字段

✅ **唯一识别码初值** - 显示当前节点的最大识别码+1
✅ **密级** - 必填，下拉选择
✅ **SNS** - 自动生成，只读显示
✅ **变量码** - 固定值"A"，只读
✅ **版本号** - 固定值"001"，只读
✅ **创作单位编码** - 必填，手动输入
✅ **责任单位编码** - 必填，手动输入
✅ **ICN分类** - 可选，手动输入

---

## 🎨 修改后的界面布局

```
┌──────────────────────────────────────────────────────┐
│                   批量新增ICN                         │
├──────────────────────────────────────────────────────┤
│ 【说明】选择多个文件后，系统将自动分配识别码...       │
├──────────────────────────────────────────────────────┤
│ 唯一识别码初值: [00001]      密级*:    [▼内部]      │
│ SNS: [01-020300]             变量码:   [A 只读]      │
│ 版本号: [001 只读]           创作单位编码*: [____]    │
│ 责任单位编码*: [____]         ICN分类:    [____]     │
├──────────────────────────────────────────────────────┤
│ 文件列表                                              │
│ [选择实体文件] [↑上移] [↓下移] [#重新填充]          │
│ ┌──┬────────────┬────────┬──────────┬──────┐       │
│ │#│ 文件名      │ 大小    │ 唯一识别码│ 操作 │       │
│ └──┴────────────┴────────┴──────────┴──────┘       │
├──────────────────────────────────────────────────────┤
│                               [确定]  [取消]         │
└──────────────────────────────────────────────────────┘
```

---

## 🔧 代码修改细节

### 1. 模板部分 (Template)

**修改前**：12个表单字段
```vue
<a-col :span="12">
  <a-form-model-item label="构型节点" required>
    <a-input :value="cmnodeName" disabled />
  </a-form-model-item>
</a-col>
<!-- ... 其他字段 -->
<a-col :span="12">
  <a-form-model-item label="创作单位名称">
    <a-input v-model="model.originatorName" />
  </a-form-model-item>
</a-col>
<a-col :span="12">
  <a-form-model-item label="责任单位名称">
    <a-input v-model="model.rpcName" />
  </a-form-model-item>
</a-col>
<a-col :span="12">
  <a-form-model-item label="安全等级">
    <a-input v-model="model.securityClassification" />
  </a-form-model-item>
</a-col>
```

**修改后**：8个表单字段
```vue
<!-- 移除了构型节点、创作单位名称、责任单位名称、安全等级 -->
<a-col :span="12">
  <a-form-model-item label="唯一识别码初值">
    <a-input :value="currentUniqueId" disabled />
  </a-form-model-item>
</a-col>
<!-- ... 其他保留字段 -->
```

### 2. 数据模型 (Data)

**修改前**：
```javascript
model: {
  sns: '',
  security: 2,
  icnType: '',
  originator: '',
  originatorName: '',      // ❌ 已移除
  rpc: '',
  rpcName: '',              // ❌ 已移除
  securityClassification: '' // ❌ 已移除
}
```

**修改后**：
```javascript
model: {
  sns: '',
  security: 2,
  icnType: '',
  originator: '',
  rpc: ''
}
```

### 3. 重置方法 (resetForm)

**修改前**：
```javascript
resetForm() {
  this.model = {
    sns: this.model.sns,
    security: 2,
    icnType: '',
    originator: '',
    originatorName: '',      // ❌ 已移除
    rpc: '',
    rpcName: '',              // ❌ 已移除
    securityClassification: '' // ❌ 已移除
  }
  // ...
}
```

**修改后**：
```javascript
resetForm() {
  this.model = {
    sns: this.model.sns,
    security: 2,
    icnType: '',
    originator: '',
    rpc: ''
  }
  // ...
}
```

### 4. 上传数据 (uploadFiles)

**修改前**：
```javascript
const dataObj = {
  cmnodeId: this.cmnodeId,
  sns: this.model.sns,
  security: this.model.security,
  variantCode: 'A',
  issueNo: '001',
  icnType: this.model.icnType,
  originator: this.model.originator,
  originatorName: this.model.originatorName,      // ❌ 已移除
  rpc: this.model.rpc,
  rpcName: this.model.rpcName,                    // ❌ 已移除
  securityClassification: this.model.securityClassification // ❌ 已移除
}
```

**修改后**：
```javascript
const dataObj = {
  cmnodeId: this.cmnodeId,
  sns: this.model.sns,
  security: this.model.security,
  variantCode: 'A',
  issueNo: '001',
  icnType: this.model.icnType,
  originator: this.model.originator,
  rpc: this.model.rpc
}
```

---

## 📊 字段对比表

| 字段名称 | 修改前 | 修改后 | 说明 |
|---------|-------|-------|------|
| 构型节点 | ✅ 显示 | ❌ 移除 | 用户已在树上选择 |
| 唯一识别码初值 | ✅ 显示 | ✅ 保留 | 显示起始值 |
| 密级 | ✅ 必填 | ✅ 保留 | 下拉选择 |
| SNS | ✅ 显示 | ✅ 保留 | 自动生成 |
| 变量码 | ✅ 显示 | ✅ 保留 | 固定"A" |
| 版本号 | ✅ 显示 | ✅ 保留 | 固定"001" |
| 创作单位编码 | ✅ 必填 | ✅ 保留 | 手动输入 |
| 创作单位名称 | ✅ 可选 | ❌ 移除 | 简化表单 |
| 责任单位编码 | ✅ 必填 | ✅ 保留 | 手动输入 |
| 责任单位名称 | ✅ 可选 | ❌ 移除 | 简化表单 |
| ICN分类 | ✅ 可选 | ✅ 保留 | 手动输入 |
| 安全等级 | ✅ 可选 | ❌ 移除 | 简化表单 |

---

## ✅ 修改效果

### 界面优化
- ✅ 表单更简洁，从12个字段减少到8个字段
- ✅ 布局更紧凑，减少了4个输入框
- ✅ 用户操作更快捷，只需填写核心必填字段

### 数据模型优化
- ✅ 减少了3个数据字段
- ✅ 简化了数据验证逻辑
- ✅ 减少了前后端数据传输量

### 用户体验提升
- ✅ 减少了用户输入工作量
- ✅ 聚焦核心字段，降低操作复杂度
- ✅ 保留了所有必填和关键字段

---

## 🔄 兼容性说明

### 后端接口
- ⚠️ 后端接口需要适配新的数据格式
- ⚠️ 确保后端不依赖移除的字段（`originatorName`, `rpcName`, `securityClassification`）
- ✅ 如果后端需要这些字段，可以在后端根据编码自动填充名称

### 数据完整性
- ✅ 必填字段未被移除
- ✅ 核心业务字段完整保留
- ⚠️ 名称字段可以后续通过编码关联查询

---

## 📋 测试检查清单

### 表单展示
- [ ] 确认只显示8个字段
- [ ] 确认移除的4个字段不再显示
- [ ] 确认必填字段标记正确（密级、创作单位编码、责任单位编码）
- [ ] 确认只读字段正确显示（唯一识别码初值、SNS、变量码、版本号）

### 数据提交
- [ ] 测试填写表单并选择文件
- [ ] 测试点击确定后数据正确提交
- [ ] 确认提交的数据中不包含移除的字段
- [ ] 确认后端接口正常接收数据

### 功能完整性
- [ ] 测试文件上传功能正常
- [ ] 测试唯一识别码管理功能正常
- [ ] 测试上移/下移功能正常
- [ ] 测试进度显示功能正常

---

## 📚 相关文档

- 批量新增功能重构说明.md
- 批量新增功能完成报告.md
- 项目实体管理功能增强说明.md

---

## 总结

✅ **修改已完成**：成功移除了4个字段，表单更加简洁
✅ **功能完整**：核心功能未受影响，所有必填字段保留
✅ **用户体验提升**：减少了用户输入工作量

**建议**：部署前与后端开发确认数据格式兼容性。
