# 批量新增弹窗布局优化说明

## 📝 优化内容

根据用户要求，对批量新增弹窗进行了三项重要优化：

### 1. ❌ 移除"重新填充识别码"按钮
- **原因**：操作频次低，可通过删除后重新添加文件实现相同效果
- **简化效果**：按钮从4个减少到3个，界面更简洁

### 2. 🔄 创作单位和责任单位改为下拉选择
- **原设计**：手动输入编码（容易出错）
- **新设计**：下拉选择（自动关联编码和名称）
- **参照**：与新增弹窗保持一致的样式和交互
- **特性**：
  - 支持搜索（show-search）
  - 自动过滤（option-filter-prop="children"）
  - 自动填充名称字段
  - 自动选中项目默认单位

### 3. 🤖 自动生成SNS
- **原设计**：手动输入或从节点继承
- **新设计**：打开弹窗时自动从后端获取
- **数据来源**：调用 `/icnmanage/ietmIcnManage/getProjectInfo` 接口
- **自动填充**：同时获取 SNS 和默认密级

---

## 🎨 优化后的界面布局

```
┌────────────────────────────────────────────────────────┐
│                   批量新增ICN                           │
├────────────────────────────────────────────────────────┤
│ 【说明】选择多个文件后，系统将自动分配识别码...         │
├────────────────────────────────────────────────────────┤
│ SNS: [01-020300 自动生成]  唯一识别码初值: [00001]     │
│ 变量码: [A 只读]           版本号: [001 只读]           │
│ 密级*: [▼内部]             ICN分类: [________]          │
│ 创作单位*: [▼选择...]      责任单位*: [▼选择...]        │
├────────────────────────────────────────────────────────┤
│ 文件列表                                                │
│ [📁选择实体文件] [↑上移] [↓下移]                       │
│ ┌──┬────────────┬────────┬──────────┬──────┐         │
│ │#│ 文件名      │ 大小    │ 唯一识别码│ 操作 │         │
│ └──┴────────────┴────────┴──────────┴──────┘         │
├────────────────────────────────────────────────────────┤
│                                  [确定]  [取消]        │
└────────────────────────────────────────────────────────┘
```

---

## 🔧 详细修改内容

### 1. 模板部分 (Template)

#### 表单字段顺序优化
**修改前**：
```vue
<a-col :span="12">
  <a-form-model-item label="唯一识别码初值">...</a-form-model-item>
</a-col>
<a-col :span="12">
  <a-form-model-item label="密级">...</a-form-model-item>
</a-col>
<a-col :span="12">
  <a-form-model-item label="SNS">...</a-form-model-item>
</a-col>
<!-- ... -->
```

**修改后**（更合理的顺序）：
```vue
<a-col :span="12">
  <a-form-model-item label="SNS">
    <a-input v-model="model.sns" disabled placeholder="自动生成" />
  </a-form-model-item>
</a-col>
<a-col :span="12">
  <a-form-model-item label="唯一识别码初值">...</a-form-model-item>
</a-col>
<!-- ... -->
```

#### 创作单位改为下拉
**修改前**：
```vue
<a-col :span="12">
  <a-form-model-item label="创作单位编码" required>
    <a-input v-model="model.originator" placeholder="请输入创作单位编码" :maxLength="5" />
  </a-form-model-item>
</a-col>
```

**修改后**：
```vue
<a-col :span="12">
  <a-form-model-item label="创作单位" required>
    <a-select
      v-model="model.originator"
      placeholder="请选择创作单位"
      show-search
      option-filter-prop="children"
      @change="handleOriginatorChange"
    >
      <a-select-option v-for="item in originatorList" :key="`originator-${item.companyCode}`" :value="item.companyCode">
        {{ item.companyName }}
      </a-select-option>
    </a-select>
  </a-form-model-item>
</a-col>
```

#### 责任单位改为下拉
**修改前**：
```vue
<a-col :span="12">
  <a-form-model-item label="责任单位编码" required>
    <a-input v-model="model.rpc" placeholder="请输入责任单位编码" :maxLength="5" />
  </a-form-model-item>
</a-col>
```

**修改后**：
```vue
<a-col :span="12">
  <a-form-model-item label="责任单位" required>
    <a-select
      v-model="model.rpc"
      placeholder="请选择责任单位"
      show-search
      option-filter-prop="children"
      @change="handleRpcChange"
    >
      <a-select-option v-for="item in rpcList" :key="`rpc-${item.companyCode}`" :value="item.companyCode">
        {{ item.companyName }}
      </a-select-option>
    </a-select>
  </a-form-model-item>
</a-col>
```

#### 移除重新填充按钮
**修改前**：
```vue
<a-button icon="arrow-up" :disabled="!canMoveUp" @click="moveUp">上移</a-button>
<a-button icon="arrow-down" :disabled="!canMoveDown" @click="moveDown">下移</a-button>
<a-button icon="number" @click="resetUniqueIds">重新填充识别码</a-button>
```

**修改后**：
```vue
<a-button icon="arrow-up" :disabled="!canMoveUp" @click="moveUp">上移</a-button>
<a-button icon="arrow-down" :disabled="!canMoveDown" @click="moveDown">下移</a-button>
```

---

### 2. 数据部分 (Data)

#### 添加单位列表
**修改前**：
```javascript
model: {
  sns: '',
  security: 2,
  icnType: '',
  originator: '',
  rpc: ''
}
```

**修改后**：
```javascript
model: {
  sns: '',
  security: 2,
  icnType: '',
  originator: '',
  originatorName: '',  // 新增：创作单位名称
  rpc: '',
  rpcName: ''          // 新增：责任单位名称
},
originatorList: [],    // 新增：创作单位列表
rpcList: []            // 新增：责任单位列表
```

---

### 3. 方法部分 (Methods)

#### show 方法 - 增加参数
**修改前**：
```javascript
show(cmnodeId, cmnodeName, sns) {
  this.visible = true
  this.cmnodeId = cmnodeId
  this.cmnodeName = cmnodeName
  this.model.sns = sns || ''
  this.resetForm()
  this.loadCurrentUniqueId()
}
```

**修改后**：
```javascript
show(cmnodeId, cmnodeName, projectInfo) {
  this.visible = true
  this.cmnodeId = cmnodeId
  this.cmnodeName = cmnodeName
  this.resetForm()
  this.loadCurrentUniqueId()
  this.loadCompanyList(projectInfo)      // 新增：加载单位列表
  this.loadProjectInfo(cmnodeId, projectInfo) // 新增：加载项目信息（SNS）
}
```

#### 新增方法：loadCompanyList
```javascript
// 加载单位列表
loadCompanyList(projectInfo) {
  const projectId = projectInfo && projectInfo.projectId
  const params = { pageNo: 1, pageSize: 1000 }
  if (projectId) {
    params.pid = projectId
  }

  getAction('/ietmprojectcompany/ietmProjectCompany/list', params).then(res => {
    if (res.success && res.result && res.result.records) {
      const allCompanies = res.result.records
      // 创作单位：type=1
      this.originatorList = allCompanies.filter(item => item.type === 1)
      // 责任单位：type=2
      this.rpcList = allCompanies.filter(item => item.type === 2)

      // 自动选中项目的创作单位和责任单位
      if (projectInfo) {
        if (projectInfo.originator) {
          this.model.originator = projectInfo.originator
          this.model.originatorName = projectInfo.originatorName
        }
        if (projectInfo.rpc) {
          this.model.rpc = projectInfo.rpc
          this.model.rpcName = projectInfo.rpcName
        }
      }
    }
  }).catch(err => {
    console.error('加载单位列表失败', err)
  })
}
```

#### 新增方法：loadProjectInfo
```javascript
// 加载项目信息（自动生成SNS）
loadProjectInfo(cmnodeId, projectInfo) {
  getAction('/icnmanage/ietmIcnManage/getProjectInfo', { cmnodeId }).then(res => {
    if (res.success) {
      this.model.sns = res.result.sns || ''
      if (!this.model.security && res.result.security) {
        this.model.security = res.result.security
      }
    }
  }).catch(err => {
    console.error('加载项目信息失败', err)
  })
}
```

#### 新增方法：单位选择变化处理
```javascript
// 创作单位选择变化
handleOriginatorChange(value) {
  const selected = this.originatorList.find(item => item.companyCode === value)
  if (selected) {
    this.model.originator = selected.companyCode
    this.model.originatorName = selected.companyName
  }
}

// 责任单位选择变化
handleRpcChange(value) {
  const selected = this.rpcList.find(item => item.companyCode === value)
  if (selected) {
    this.model.rpc = selected.companyCode
    this.model.rpcName = selected.companyName
  }
}
```

#### 移除方法：resetUniqueIds
```javascript
// ❌ 已移除
resetUniqueIds() {
  this.fileList.forEach((file, index) => {
    file.uniqueId = this.currentUniqueIdNum + index
  })
  this.$message.success('已按顺序重新填充唯一识别码')
}
```

#### 更新验证提示
**修改前**：
```javascript
if (!this.model.originator) {
  this.$message.warning('请输入创作单位编码')
  return
}

if (!this.model.rpc) {
  this.$message.warning('请输入责任单位编码')
  return
}
```

**修改后**：
```javascript
if (!this.model.originator) {
  this.$message.warning('请选择创作单位')
  return
}

if (!this.model.rpc) {
  this.$message.warning('请选择责任单位')
  return
}
```

---

### 4. 主列表调用修改

**文件**：`IetmIcnManageList.vue`

**修改前**：
```javascript
handleBatchAdd() {
  // ...
  const nodeName = currentNode ? currentNode.title : ''
  this.$refs.batchAddModal.show(this.currentCmnodeId, nodeName)
}
```

**修改后**：
```javascript
handleBatchAdd() {
  // ...
  const nodeName = currentNode ? currentNode.title : ''
  this.$refs.batchAddModal.show(this.currentCmnodeId, nodeName, this.currentProjectInfo)
}
```

---

## 📊 优化对比

### 功能对比

| 项目 | 优化前 | 优化后 | 改进 |
|------|-------|-------|------|
| 创作单位输入方式 | 手动输入编码 | 下拉选择 | ✅ 避免输入错误 |
| 责任单位输入方式 | 手动输入编码 | 下拉选择 | ✅ 避免输入错误 |
| SNS生成方式 | 手动输入 | 自动获取 | ✅ 减少操作 |
| 默认单位选择 | 无 | 自动选中项目单位 | ✅ 提升效率 |
| 按钮数量 | 4个 | 3个 | ✅ 界面简洁 |
| 搜索功能 | 无 | 支持下拉搜索 | ✅ 快速查找 |

### 用户体验提升

| 维度 | 提升内容 |
|------|---------|
| **操作效率** | 自动填充SNS和默认单位，减少输入 |
| **错误率** | 下拉选择避免编码输入错误 |
| **易用性** | 支持搜索，快速找到目标单位 |
| **一致性** | 与新增弹窗保持一致的交互方式 |
| **界面简洁** | 减少不常用按钮，突出核心功能 |

---

## 🔌 API 接口依赖

### 新增接口调用

1. **获取单位列表**
   - 接口：`/ietmprojectcompany/ietmProjectCompany/list`
   - 参数：`{ pageNo: 1, pageSize: 1000, pid: projectId }`
   - 返回：单位列表（type: 1=创作单位, 2=责任单位）

2. **获取项目信息**
   - 接口：`/icnmanage/ietmIcnManage/getProjectInfo`
   - 参数：`{ cmnodeId }`
   - 返回：`{ sns, security, uniqueId }`

---

## 🧪 测试建议

### 功能测试

1. **SNS自动生成**
   - 打开批量新增弹窗
   - 验证SNS字段自动填充
   - 验证SNS值正确对应节点路径

2. **单位下拉选择**
   - 点击创作单位下拉框
   - 验证列表显示项目关联的创作单位
   - 选择一个单位，验证编码和名称都正确赋值
   - 测试搜索功能（输入单位名称关键字）

3. **默认单位选择**
   - 打开批量新增弹窗
   - 验证创作单位和责任单位自动选中项目默认值

4. **按钮功能**
   - 验证"重新填充识别码"按钮已移除
   - 验证上移/下移按钮正常工作

### 边界测试

1. **空列表情况**
   - 项目没有关联单位时的处理
   - 验证下拉框显示为空

2. **网络异常**
   - 单位列表加载失败的处理
   - SNS获取失败的处理

3. **兼容性**
   - 旧数据迁移（编码输入→下拉选择）
   - 确保提交数据格式正确

---

## 📝 注意事项

### 后端兼容性

✅ **无需后端修改**：
- 上传接口数据格式不变
- 仍然传递 `originator`、`rpc` 编码
- 额外传递 `originatorName`、`rpcName` 名称

⚠️ **依赖接口**：
- 需要确保 `/ietmprojectcompany/ietmProjectCompany/list` 接口可用
- 需要确保 `/icnmanage/ietmIcnManage/getProjectInfo` 接口可用

### 数据完整性

- ✅ 单位编码和名称自动关联
- ✅ 避免编码和名称不匹配
- ✅ 自动选择项目默认单位

---

## 📚 相关文档

- 批量新增功能重构说明.md
- 批量新增功能完成报告.md
- 批量新增弹窗字段调整说明.md

---

## ✨ 总结

### 已完成的优化

1. ✅ **移除"重新填充识别码"按钮** - 简化操作
2. ✅ **创作单位改为下拉选择** - 避免输入错误，支持搜索
3. ✅ **责任单位改为下拉选择** - 避免输入错误，支持搜索
4. ✅ **自动生成SNS** - 减少用户输入，自动从后端获取
5. ✅ **自动选中默认单位** - 提升效率，减少重复操作
6. ✅ **与新增弹窗保持一致** - 统一交互体验

### 优化效果

- 📉 **用户输入量减少**：3个输入字段 → 0个（全部改为选择或自动填充）
- 📈 **数据准确性提升**：避免手动输入编码错误
- 🎯 **操作效率提升**：自动填充SNS和默认单位
- 🤝 **交互一致性提升**：与新增弹窗保持一致

**优化完成，可以直接使用！** 🎉
