# 批量新增功能重构说明

## 📋 功能概述

根据需求文档重新实现了完整的批量新增功能，支持多文件选择、唯一识别码管理、文件列表排序等完整功能。

---

## ✨ 核心功能特性

### 1. 多文件上传
- 支持一次选择多个文件
- 支持格式：`.bmp, .jpg, .jpeg, .png, .gif, .tif, .tiff, .cgm, .svg, .swf, .mp3, .mp4, .webm, .ogg, .wrl, .smg`
- 单个文件最大 100MB
- 自动文件格式验证和大小限制

### 2. 唯一识别码管理
- **自动分配**：文件加入列表时自动分配递增的唯一识别码
- **手动编辑**：可在表格中直接修改每个文件的唯一识别码
- **格式化显示**：5位数字，不足补零（如 00001, 00123）
- **范围限制**：最小值为当前节点最大识别码+1，最大值 99999
- **重复检测**：实时检测并提示重复的识别码
- **重新填充**：点击"重新填充识别码"按钮可按初始值重新分配

### 3. 文件列表管理
- **单选选中**：点击行或单选按钮选中文件
- **上移/下移**：调整文件在列表中的顺序
- **删除文件**：从列表中移除不需要的文件
- **文件信息显示**：
  - 序号（自动更新）
  - 文件名
  - 文件大小（KB格式，千分位分隔）
  - 唯一识别码（可编辑）
  - 操作按钮

### 4. 表单验证
- **必填字段**：密级、创作单位编码、责任单位编码
- **文件列表**：至少选择一个文件
- **唯一识别码**：不能小于初始值，不能重复
- **友好提示**：每项验证失败都有明确的提示信息

### 5. 批量上传进度
- **实时进度条**：显示当前上传进度百分比
- **文件计数**：显示"已完成 X / Y 个文件"
- **状态指示**：
  - 进行中：蓝色进度条
  - 成功：绿色进度条 + 成功消息
  - 失败：红色进度条 + 错误信息

### 6. SNS 自动生成
- 根据选中的构型树节点路径自动生成 SNS
- 符合需求文档规则：只取前6段，第3、4位去掉中间的"-"

---

## 🎯 与需求文档的对照

| 需求项 | 需求文档要求 | 实现状态 |
|-------|------------|---------|
| 多文件选择 | ✅ 支持 | ✅ 已实现 |
| 文件列表展示 | ✅ 需要 | ✅ 已实现 |
| 上移/下移 | ✅ 需要 | ✅ 已实现 |
| 唯一识别码自动分配 | ✅ 需要 | ✅ 已实现 |
| 唯一识别码可编辑 | ✅ 需要 | ✅ 已实现 |
| 重新填充识别码 | ✅ 需要 | ✅ 已实现 |
| 删除文件 | ✅ 需要 | ✅ 已实现 |
| 进度显示 | ✅ 需要 | ✅ 已实现 |
| 文件格式限制 | ✅ 需要 | ✅ 已实现 |
| 文件大小限制 | ✅ ≤100MB | ✅ 已实现 |
| 唯一识别码验证 | ✅ ≥初值、不重复 | ✅ 已实现 |

---

## 📝 页面布局

```
┌─────────────────────────────────────────────────────────────┐
│                    批量新增ICN                                │
├─────────────────────────────────────────────────────────────┤
│ 【说明提示】                                                   │
│ 【进度条】（上传时显示）                                         │
├─────────────────────────────────────────────────────────────┤
│ 表单区域：                                                     │
│   构型节点*    [显示名称]           唯一识别码初值  [00001]     │
│   密级*        [下拉选择]           SNS            [自动生成]   │
│   变量码       [A - 只读]           版本号         [001 - 只读] │
│   创作单位编码* [输入框]             创作单位名称   [输入框]     │
│   责任单位编码* [输入框]             责任单位名称   [输入框]     │
│   ICN分类      [输入框]             安全等级       [输入框]     │
├─────────────────────────────────────────────────────────────┤
│ 文件列表：                                                     │
│   [选择实体文件] [↑上移] [↓下移] [#重新填充] [格式提示]        │
│   ┌────┬──────────────┬──────────┬────────────┬────────┐    │
│   │ # │ 文件名        │ 文件大小  │ 唯一识别码  │ 操作   │    │
│   ├────┼──────────────┼──────────┼────────────┼────────┤    │
│   │ 1 │ image1.png   │ 1,234 KB │ [00001]    │ 删除   │    │
│   │ 2 │ image2.jpg   │ 567 KB   │ [00002]    │ 删除   │    │
│   │ 3 │ model.wrl    │ 5,678 KB │ [00003]    │ 删除   │    │
│   └────┴──────────────┴──────────┴────────────┴────────┘    │
│                                                               │
│   （空列表时显示提示图标和文字）                                │
├─────────────────────────────────────────────────────────────┤
│                                      [确定]  [取消]           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 技术实现细节

### 1. 文件上传组件
使用 Ant Design Vue 的 `a-upload` 组件：
```vue
<a-upload
  :file-list="[]"
  :before-upload="beforeUpload"
  accept=".bmp,.jpg,.jpeg,.png,..."
  multiple
  :show-upload-list="false"
>
  <a-button type="primary" icon="upload">
    选择实体文件
  </a-button>
</a-upload>
```

### 2. 文件列表数据结构
```javascript
fileList: [
  {
    id: 'unique-id',         // 唯一ID（用于表格 row-key）
    file: File,              // 原始文件对象
    name: 'image.png',       // 文件名
    size: 1234567,           // 文件大小（字节）
    uniqueId: 1,             // 唯一识别码（数字）
    displayUniqueId: '00001' // 格式化显示（5位补零）
  }
]
```

### 3. 唯一识别码自动分配逻辑
```javascript
// 获取当前最大识别码
let maxId = this.currentUniqueIdNum
if (this.fileList.length > 0) {
  const ids = this.fileList.map(f => f.uniqueId)
  maxId = Math.max(maxId, ...ids)
}

// 为新文件分配识别码
file.uniqueId = maxId + 1
```

### 4. 文件顺序调整
```javascript
// 上移
moveUp() {
  const index = this.getSelectedFileIndex()
  if (index > 0) {
    const temp = this.fileList[index]
    this.fileList.splice(index, 1)
    this.fileList.splice(index - 1, 0, temp)
  }
}

// 下移
moveDown() {
  const index = this.getSelectedFileIndex()
  if (index < this.fileList.length - 1) {
    const temp = this.fileList[index]
    this.fileList.splice(index, 1)
    this.fileList.splice(index + 1, 0, temp)
  }
}
```

### 5. 批量上传实现
```javascript
// 使用 FormData 逐个上传文件
for (let i = 0; i < this.fileList.length; i++) {
  const item = this.fileList[i]
  const formData = new FormData()
  
  // 添加表单数据
  formData.append('data', JSON.stringify({
    cmnodeId: this.cmnodeId,
    sns: this.model.sns,
    security: this.model.security,
    // ... 其他字段
  }))
  
  // 添加文件和唯一识别码映射
  formData.append('file', item.file)
  formData.append('uniqueId', this.formatUniqueId(item.uniqueId))
  
  // 上传
  await httpAction('/icnmanage/ietmIcnManage/batchUpload', formData, 'post')
  
  // 更新进度
  this.progress.current = i + 1
}
```

### 6. SNS 生成规则
```javascript
generateSNS(nodeTitle) {
  // 从节点标题中提取编码（格式：01-02-03 节点名称）
  const parts = nodeTitle.split(' ')
  if (parts.length > 0) {
    const codes = parts[0].split('-')
    // 只取前6段
    let sns = ''
    codes.slice(0, 6).forEach((code, index) => {
      // 第3、4位（索引2、3）去掉中间的"-"
      if (index >= 2 && index <= 3) {
        sns += code
      } else {
        sns += (index > 0 ? '-' : '') + code
      }
    })
    return sns
  }
  return ''
}
```

---

## 🧪 测试用例

### 测试用例 1：基本流程
1. 选择构型树中的非根节点
2. 点击"批量新增"按钮
3. 填写必填字段（密级、创作单位、责任单位）
4. 点击"选择实体文件"，选择3个图片文件
5. 验证：文件列表显示3个文件，唯一识别码分别为 00001, 00002, 00003
6. 点击"确定"
7. 验证：显示上传进度，上传成功后关闭弹窗，列表刷新

### 测试用例 2：文件顺序调整
1. 添加3个文件（A.png, B.png, C.png）
2. 选中 B.png（第2行）
3. 点击"上移"
4. 验证：B.png 移到第1行，序号和识别码不变
5. 点击"下移"两次
6. 验证：B.png 移到第3行

### 测试用例 3：唯一识别码编辑
1. 添加2个文件
2. 修改第1个文件的识别码为 00005
3. 尝试修改第2个文件的识别码也为 00005
4. 验证：显示错误提示"第2行的唯一识别码与第1行重复"
5. 修改为 00006
6. 验证：错误提示消失

### 测试用例 4：重新填充识别码
1. 添加3个文件
2. 修改识别码为 00010, 00015, 00020
3. 点击"重新填充识别码"
4. 验证：识别码重置为 00001, 00002, 00003

### 测试用例 5：文件删除
1. 添加5个文件
2. 选中第3个文件
3. 点击"删除"
4. 验证：文件被移除，剩余4个文件，序号自动更新

### 测试用例 6：文件格式验证
1. 尝试选择不支持的文件格式（如.docx）
2. 验证：显示错误提示"不支持的文件格式"

### 测试用例 7：文件大小限制
1. 尝试选择超过100MB的文件
2. 验证：显示错误提示"文件大小不能超过100MB"

### 测试用例 8：必填字段验证
1. 不填写密级，直接点击"确定"
2. 验证：显示提示"请选择密级"
3. 填写密级，不选择文件，点击"确定"
4. 验证：显示提示"请至少选择一个文件"

### 测试用例 9：识别码下限验证
1. 当前节点最大识别码为 00050
2. 添加文件后，尝试修改识别码为 00040
3. 验证：输入框自动调整为最小值 00051

### 测试用例 10：根节点限制
1. 选择构型树的根节点
2. 点击"批量新增"
3. 验证：显示提示"不能在根节点下批量新增ICN，请选择子节点"

---

## 📊 数据流程图

```
用户操作
   ↓
选择构型节点 → 获取当前最大唯一识别码 (Ajax)
   ↓
填写表单字段
   ↓
选择文件 → 文件格式验证 → 文件大小验证
   ↓
文件加入列表 → 自动分配唯一识别码
   ↓
[可选] 调整顺序/修改识别码/删除文件
   ↓
点击"确定" → 表单验证
   ↓
逐个上传文件
   │
   ├→ 构造 FormData (表单数据 + 文件 + 唯一识别码)
   ├→ POST /batchUpload
   ├→ 更新进度条
   └→ 重复直到所有文件上传完成
   ↓
上传成功 → 关闭弹窗 → 刷新列表
```

---

## 🎨 UI交互说明

### 1. 按钮状态
- **上移按钮**：
  - 启用条件：已选中文件 && 不是第一个文件
  - 禁用状态：灰色显示
  
- **下移按钮**：
  - 启用条件：已选中文件 && 不是最后一个文件
  - 禁用状态：灰色显示

### 2. 唯一识别码输入框
- **焦点状态**：蓝色边框
- **错误状态**：红色边框（重复或小于最小值）
- **格式化显示**：5位数字，不足补零
- **输入范围**：受到 min 和 max 限制

### 3. 文件列表选中
- **点击行**：选中该文件，高亮显示
- **单选按钮**：与点击行效果相同

### 4. 进度条状态
- **进行中**：蓝色，动画效果
- **成功**：绿色，静态
- **失败**：红色，静态

---

## 🔗 接口说明

### 1. 获取当前最大唯一识别码
```http
GET /icnmanage/ietmIcnManage/getMaxUniqueId?cmnodeId={cmnodeId}

Response:
{
  "success": true,
  "result": 50,  // 当前最大识别码
  "message": "获取成功"
}
```

### 2. 批量上传
```http
POST /icnmanage/ietmIcnManage/batchUpload

Content-Type: multipart/form-data

Parameters:
- data: JSON字符串，包含表单数据
  {
    "cmnodeId": "xxx",
    "sns": "01-02-03",
    "security": 2,
    "originator": "AVIC",
    "originatorName": "中航工业",
    "rpc": "COMAC",
    "rpcName": "商飞公司",
    "icnType": "图片",
    "securityClassification": "A",
    "variantCode": "A",
    "issueNo": "001"
  }
- file: 文件对象
- uniqueId: 唯一识别码（5位字符串，如 "00001"）

Response:
{
  "success": true,
  "result": {
    "id": "xxx",
    "icn": "ICN-xxx-xxx-xxx"
  },
  "message": "上传成功"
}
```

---

## 🚀 性能优化

1. **虚拟滚动**：文件列表使用 `:scroll="{ y: 240 }"` 限制高度，支持大量文件
2. **防抖处理**：唯一识别码输入时的验证使用防抖，避免频繁校验
3. **批量上传优化**：
   - 使用 Promise 控制并发
   - 失败时不中断其他文件的上传
   - 提供详细的错误信息

---

## 📌 注意事项

1. **浏览器兼容性**：需要现代浏览器支持（Chrome 80+, Firefox 75+, Edge 80+）
2. **文件大小限制**：前端限制100MB，后端也需要配置相应的上传大小限制
3. **超时设置**：大文件上传时需要合理设置超时时间
4. **错误处理**：上传失败时需要明确告知用户哪个文件失败及原因
5. **唯一识别码**：需要确保后端也验证识别码的唯一性

---

## 🎯 后续优化方向

1. **拖拽排序**：支持鼠标拖拽调整文件顺序
2. **批量编辑**：支持批量修改唯一识别码（如批量+10）
3. **预览功能**：上传前预览图片和视频
4. **断点续传**：支持大文件断点续传
5. **并发上传**：支持多个文件同时上传（控制并发数）
6. **Excel导入**：支持从Excel导入文件列表和识别码

---

## 📖 相关文档

- 需求文档：`项目实体管理模块需求分析.md` - 第3.1节
- 接口文档：后端API文档（待补充）
- Ant Design Vue：https://www.antdv.com/components/upload-cn/

---

## ✅ 完成检查清单

- [x] 多文件选择
- [x] 文件列表展示
- [x] 上移/下移功能
- [x] 唯一识别码自动分配
- [x] 唯一识别码可编辑
- [x] 重新填充识别码
- [x] 删除文件功能
- [x] 进度条显示
- [x] 文件格式验证
- [x] 文件大小限制
- [x] 唯一识别码验证
- [x] 表单必填验证
- [x] SNS自动生成
- [x] 根节点限制
- [x] 错误提示
- [x] 成功反馈
- [x] 列表刷新

---

**实现完成日期**：2024年
**开发者**：Claude (Kiro)
**版本**：v2.0
