# 前置条件修复 - 真实UI测试手册

**测试目标**: 通过真实浏览器UI交互验证所有前置条件校验  
**测试原则**: 不绕过Vue层，100%真实用户操作  
**预计时间**: 30-40分钟

---

## 🔧 测试环境准备

### 步骤1: 启动后端服务

```bash
# 进入后端目录
cd /d/workspace/IETM/cape-ietm-java

# 启动Spring Boot应用
mvn spring-boot:run -pl jeecg-system-start

# 或使用已打包的jar
java -jar jeecg-system-start/target/jeecg-system-start-3.4.4.jar

# 验证后端已启动
# 浏览器访问: http://localhost:9999/jeecg-boot/sys/actuator/health
```

### 步骤2: 启动前端服务

```bash
# 进入前端目录
cd /d/workspace/IETM/cape-ietm-vue

# 启动开发服务器
npm run serve

# 等待编译完成，看到:
# App running at:
# - Local:   http://localhost:3000/
```

### 步骤3: 验证环境

- ✅ 后端健康检查: `http://localhost:9999/jeecg-boot/sys/actuator/health`
- ✅ 前端首页访问: `http://localhost:3000/`
- ✅ 登录系统: 用户名 `admin` / 密码 `admin123`

---

## 📋 测试场景设计

### 场景分类

| 分类 | 场景数 | 预计时间 |
|------|--------|---------|
| P0 - 工作流前置校验 | 6个 | 15分钟 |
| P1 - 并发场景 | 3个 | 10分钟 |
| P2 - 边界条件 | 4个 | 10分钟 |
| P3 - 项目参数校验 | 4个 | 5分钟 |

---

## 🧪 P0 - 工作流前置校验测试

### 测试数据准备

**需要准备的DM记录**:
1. DM_A: 未启动工作流（workflowInstanceId=null）
2. DM_B: 已启动工作流，节点="审核"
3. DM_C: 已启动工作流，节点="DM编写"，未签出
4. DM_D: 已启动工作流，节点="DM编写"，已被用户A签出
5. DM_E: 已启动工作流，节点="已结束"

**准备方法**:
```sql
-- 如果数据库中没有上述状态的DM，手工创建或修改
-- 示例：将某个DM的工作流状态设为null
UPDATE ietm_data_module SET workflow_instance_id = NULL WHERE id = 'xxx';
```

---

### TC-P0-01: 未启动工作流签出应拒绝

**前置条件**:
- 已登录系统
- 列表中存在DM_A（未启动工作流）

**测试步骤**:
1. 进入"项目数据模块管理"页面: `菜单 → 项目管理 → 项目数据模块管理`
2. 找到DM_A，查看"流程状态"列显示"未启动"或空
3. 勾选DM_A的复选框（点击行首checkbox）
4. 点击工具栏"签出"按钮
5. **观察弹窗/提示消息**

**预期结果**:
- ✅ 立即显示警告消息（黄色）："该DM还未启动流程，不能签出"
- ✅ **不应弹出**"签出确认"对话框
- ✅ 列表状态不变（DM_A仍未被签出）

**实际结果**: ____________

**截图**: 保存到 `test-evidence/TC-P0-01.png`

---

### TC-P0-02: 非DM编写节点签出应拒绝

**前置条件**:
- 已登录系统
- 列表中存在DM_B（节点="审核"）

**测试步骤**:
1. 进入"项目数据模块管理"页面
2. 找到DM_B，查看"流程当前步骤"列显示"审核"
3. 勾选DM_B的复选框
4. 点击工具栏"签出"按钮
5. **观察弹窗/提示消息**

**预期结果**:
- ✅ 立即显示警告消息："当前流程节点不是'DM编写'，不能签出"
- ✅ **不应弹出**"签出确认"对话框

**实际结果**: ____________

**截图**: `test-evidence/TC-P0-02.png`

---

### TC-P0-03: 已结束流程签出应拒绝

**前置条件**:
- 列表中存在DM_E（节点="已结束"）

**测试步骤**:
1. 找到DM_E，查看"流程状态"列显示"已结束"
2. 勾选DM_E的复选框
3. 点击工具栏"签出"按钮

**预期结果**:
- ✅ 显示警告消息："当前流程节点不是'DM编写'，不能签出"
- ✅ **不应弹出**确认对话框

**实际结果**: ____________

---

### TC-P0-04: 合法场景签出应成功

**前置条件**:
- 列表中存在DM_C（节点="DM编写"，未签出）

**测试步骤**:
1. 找到DM_C，确认:
   - "流程当前步骤"列 = "DM编写"
   - "签出人"列 = 空
2. 勾选DM_C的复选框
3. 点击工具栏"签出"按钮
4. **观察确认对话框内容**
5. 查看对话框中的版本信息（当前版本 → 新版本）
6. 点击"确定"按钮
7. 等待签出完成
8. **刷新页面**，查看DM_C状态

**预期结果**:
- ✅ 应弹出"签出确认"对话框
- ✅ 对话框显示版本变化（如：001-00 → 001-01）
- ✅ 点击确定后显示成功提示（绿色）："签出成功！..."
- ✅ 刷新后DM_C的"签出人"列 = 当前用户名
- ✅ 列表中出现新版本DM（inWork+1）

**实际结果**: ____________

**截图**: 
- `test-evidence/TC-P0-04-dialog.png` (确认对话框)
- `test-evidence/TC-P0-04-success.png` (成功提示)
- `test-evidence/TC-P0-04-result.png` (刷新后状态)

---

### TC-P0-05: 已被签出DM再次签出应拒绝

**前置条件**:
- DM_D已被用户A签出

**测试步骤**:
1. 找到DM_D，查看"签出人"列显示用户名
2. 勾选DM_D的复选框
3. 点击工具栏"签出"按钮

**预期结果**:
- ✅ 显示警告消息："该DM已由【用户A】签出，您只能浏览"
- ✅ 或显示错误："数据模块已被用户 xxx 签出"

**实际结果**: ____________

---

### TC-P0-06: 后端校验验证（绕过前端直接调API）

**目的**: 验证后端兜底校验是否生效

**测试步骤**:
1. 打开浏览器开发者工具（F12）
2. 切换到"网络(Network)"标签页
3. 找到一个未启动工作流的DM的ID（如：DM_A的ID）
4. 在Console中执行直接API调用:
   ```javascript
   // 绕过前端校验，直接调用签出API
   fetch('http://localhost:9999/jeecg-boot/ietm/datamodule/checkOut?id=<DM_A的ID>', {
     method: 'POST',
     headers: {
       'X-Access-Token': localStorage.getItem('Vue-Access-Token'),
       'Content-Type': 'application/json'
     }
   })
   .then(res => res.json())
   .then(data => console.log('后端响应:', data))
   ```
5. **查看Console输出的响应**

**预期结果**:
- ✅ 响应 `success: false`
- ✅ 响应 `message: "数据模块未启动工作流，不能签出"`
- ✅ 列表状态不变（签出失败）

**实际结果**: ____________

**截图**: `test-evidence/TC-P0-06-console.png`

---

## 🔄 P1 - 并发场景测试

### TC-P1-01: 签出确认期间工作流被取消

**模拟场景**: 用户A点击签出 → 确认对话框弹出 → 期间管理员撤销了工作流 → 用户A点击确定

**测试步骤**:
1. 用户A: 勾选DM_C，点击"签出"
2. 等待确认对话框弹出，**不要点击确定**
3. 管理员: 在另一个浏览器标签页/窗口中，撤销DM_C的工作流
   ```sql
   -- 或者直接修改数据库
   UPDATE ietm_data_module SET workflow_instance_id = NULL WHERE id = '<DM_C的ID>';
   ```
4. 用户A: 点击确认对话框的"确定"按钮
5. **观察提示消息**

**预期结果**:
- ✅ 显示错误消息（红色）："该DM还未启动流程"
- ✅ 列表自动刷新
- ✅ 签出失败（未创建新版本）

**实际结果**: ____________

**截图**: `test-evidence/TC-P1-01.png`

---

### TC-P1-02: 签出确认期间流程流转到下一节点

**模拟场景**: 确认对话框期间，流程从"DM编写"流转到"审核"

**测试步骤**:
1. 用户A: 勾选DM_C，点击"签出"，等待确认对话框
2. 管理员: 在工作流管理中，将DM_C流转到"审核"节点
   ```sql
   UPDATE ietm_data_module SET workflow_step = '审核' WHERE id = '<DM_C的ID>';
   ```
3. 用户A: 点击"确定"

**预期结果**:
- ✅ 显示错误消息："当前流程节点不是'DM编写'"
- ✅ 列表自动刷新
- ✅ 签出失败

**实际结果**: ____________

---

### TC-P1-03: 签出确认期间DM被他人签出

**模拟场景**: A用户弹出确认框期间，B用户抢先签出

**测试步骤**:
1. 用户A: 勾选DM_C，点击"签出"，等待确认对话框
2. 用户B: 在另一浏览器中，签出同一DM_C（如果成功）
   - 或者数据库修改: `UPDATE ietm_data_module SET checkout_user = 'userB' WHERE id = '<DM_C的ID>';`
3. 用户A: 点击"确定"

**预期结果**:
- ✅ 显示错误消息："该DM已被用户 userB 签出"
- ✅ 或显示："数据模块已被用户 userB 签出"
- ✅ 列表刷新，显示DM_C已被userB签出

**实际结果**: ____________

---

## 🎯 P2 - 边界条件测试

### TC-P2-01: 空字符串工作流ID

**测试步骤**:
1. 数据库修改:
   ```sql
   UPDATE ietm_data_module SET workflow_instance_id = '' WHERE id = '<测试DM的ID>';
   ```
2. 刷新列表页
3. 勾选该DM，点击"签出"

**预期结果**:
- ✅ 显示："该DM还未启动流程，不能签出"
- ✅ `oConvertUtils.isEmpty("")` 应返回true

**实际结果**: ____________

---

### TC-P2-02: 空字符串节点名称

**测试步骤**:
1. 数据库修改:
   ```sql
   UPDATE ietm_data_module 
   SET workflow_instance_id = 'wf_test_001', workflow_step = '' 
   WHERE id = '<测试DM的ID>';
   ```
2. 刷新列表页
3. 勾选该DM，点击"签出"

**预期结果**:
- ✅ 显示："当前流程节点不是'DM编写'，不能签出"
- ✅ `"DM编写".equals("")` 应返回false

**实际结果**: ____________

---

### TC-P2-03: 节点名称大小写敏感

**测试步骤**:
1. 数据库修改:
   ```sql
   UPDATE ietm_data_module 
   SET workflow_step = 'dm编写'  -- 小写
   WHERE id = '<测试DM的ID>';
   ```
2. 刷新列表页
3. 勾选该DM，点击"签出"

**预期结果**:
- ✅ 显示："当前流程节点不是'DM编写'，不能签出"
- ✅ 校验应区分大小写

**实际结果**: ____________

---

### TC-P2-04: 节点名称前后空格

**测试步骤**:
1. 数据库修改:
   ```sql
   UPDATE ietm_data_module 
   SET workflow_step = ' DM编写 '  -- 前后有空格
   WHERE id = '<测试DM的ID>';
   ```
2. 刷新列表页
3. 勾选该DM，点击"签出"

**预期结果**:
- ⚠️ 可能显示："当前流程节点不是'DM编写'"
- 📋 建议：前端应trim处理，或后端数据入库时trim

**实际结果**: ____________

**备注**: 如果失败，记录为潜在bug

---

## 🔧 P3 - 项目参数格式校验测试

### 测试数据准备

找到或创建一个测试项目，进入"项目参数配置"页面。

**导航路径**: `项目管理 → 项目列表 → 选择项目 → 参数配置`

---

### TC-P3-01: cageCode非法格式（少于5位）

**测试步骤**:
1. 进入项目参数配置页面
2. 找到"cageCode"参数输入框
3. 输入"123"（3位，少于规定的5位）
4. 点击"保存"按钮
5. **观察错误提示**

**预期结果**:
- ✅ 显示错误消息（红色）："cageCode 必须为5位字母或数字"
- ✅ 保存失败，表单停留在当前页面

**实际结果**: ____________

**截图**: `test-evidence/TC-P3-01.png`

---

### TC-P3-02: cageCode非法格式（包含特殊字符）

**测试步骤**:
1. cageCode输入框输入"12-AB"（包含短横线）
2. 点击"保存"

**预期结果**:
- ✅ 显示错误："cageCode 必须为5位字母或数字"
- ✅ 正则 `/^[A-Za-z0-9]{5}$/` 应拒绝特殊字符

**实际结果**: ____________

---

### TC-P3-03: countryCode非法格式

**测试步骤**:
1. countryCode输入框输入"C"（1位，应为2位）
2. 点击"保存"

**预期结果**:
- ✅ 显示错误："countryCode 必须为2位字母"

**实际结果**: ____________

---

### TC-P3-04: languageCode边界值

**测试步骤**:
1. 测试2位: languageCode输入"zh" → 保存 → 应成功 ✅
2. 测试3位: languageCode输入"eng" → 保存 → 应成功 ✅
3. 测试1位: languageCode输入"z" → 保存 → 应失败 ❌
4. 测试4位: languageCode输入"chin" → 保存 → 应失败 ❌

**预期结果**:
- ✅ 2-3位通过，其他位数拒绝
- ✅ 正则 `/^[A-Za-z]{2,3}$/` 正确工作

**实际结果**: ____________

---

## 📊 测试结果汇总表

### P0 - 工作流前置校验

| 用例编号 | 测试场景 | 预期结果 | 实际结果 | 通过/失败 | 备注 |
|---------|---------|---------|---------|----------|------|
| TC-P0-01 | 未启动工作流签出 | 拒绝 | _______ | ☐ | |
| TC-P0-02 | 非DM编写节点签出 | 拒绝 | _______ | ☐ | |
| TC-P0-03 | 已结束流程签出 | 拒绝 | _______ | ☐ | |
| TC-P0-04 | 合法场景签出 | 成功 | _______ | ☐ | |
| TC-P0-05 | 已签出再次签出 | 拒绝 | _______ | ☐ | |
| TC-P0-06 | 后端API校验 | 拒绝 | _______ | ☐ | |

### P1 - 并发场景

| 用例编号 | 测试场景 | 预期结果 | 实际结果 | 通过/失败 | 备注 |
|---------|---------|---------|---------|----------|------|
| TC-P1-01 | 确认期间工作流取消 | 拒绝 | _______ | ☐ | |
| TC-P1-02 | 确认期间节点流转 | 拒绝 | _______ | ☐ | |
| TC-P1-03 | 确认期间被他人签出 | 拒绝 | _______ | ☐ | |

### P2 - 边界条件

| 用例编号 | 测试场景 | 预期结果 | 实际结果 | 通过/失败 | 备注 |
|---------|---------|---------|---------|----------|------|
| TC-P2-01 | 空字符串工作流ID | 拒绝 | _______ | ☐ | |
| TC-P2-02 | 空字符串节点名称 | 拒绝 | _______ | ☐ | |
| TC-P2-03 | 节点大小写敏感 | 拒绝 | _______ | ☐ | |
| TC-P2-04 | 节点前后空格 | 拒绝? | _______ | ☐ | 可能是bug |

### P3 - 项目参数格式校验

| 用例编号 | 测试场景 | 预期结果 | 实际结果 | 通过/失败 | 备注 |
|---------|---------|---------|---------|----------|------|
| TC-P3-01 | cageCode少于5位 | 拒绝 | _______ | ☐ | |
| TC-P3-02 | cageCode特殊字符 | 拒绝 | _______ | ☐ | |
| TC-P3-03 | countryCode非2位 | 拒绝 | _______ | ☐ | |
| TC-P3-04 | languageCode边界 | 2-3位通过 | _______ | ☐ | |

### 总体统计

- **总用例数**: 17
- **通过数**: _______
- **失败数**: _______
- **通过率**: _______

---

## 🐛 发现的问题

### 问题登记表

| 问题编号 | 测试用例 | 问题描述 | 严重程度 | 复现步骤 | 截图 |
|---------|---------|---------|---------|---------|------|
| BUG-001 | _______ | _______ | P0/P1/P2 | _______ | _______ |
| BUG-002 | _______ | _______ | P0/P1/P2 | _______ | _______ |

---

## 📸 证据收集

所有测试截图保存到目录: `/d/workspace/IETM/cape-ietm-vue/test-evidence/`

**命名规则**:
- 成功案例: `TC-P0-04-success.png`
- 失败案例: `TC-P0-01-warning.png`
- Console日志: `TC-P0-06-console.png`

---

## ✅ 测试完成检查清单

测试执行前:
- [ ] 后端服务已启动（端口9999）
- [ ] 前端服务已启动（端口3000）
- [ ] 数据库连接正常
- [ ] 测试数据已准备（5种状态的DM）
- [ ] 测试账号已登录
- [ ] 截图工具已准备

测试执行中:
- [ ] 每个用例都截图
- [ ] 填写实际结果
- [ ] 记录异常现象
- [ ] Console日志保存

测试执行后:
- [ ] 填写汇总表
- [ ] 计算通过率
- [ ] 登记发现的问题
- [ ] 整理证据文件
- [ ] 编写测试报告

---

**测试手册版本**: v1.0  
**编写时间**: 2026-08-13  
**编写人员**: Claude  
**预计测试时间**: 30-40分钟
